{
  "name": "ipad-tools",
  "version": "1.0.0",
  "private": true,
  "description": "Dev tooling for ScriptCalc (index.html). The app itself has no build step and no runtime dependencies.",
  "scripts": {
    "setup": "node scripts/download-libs.mjs",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "esbuild": "^0.24.0",
    "luxon": "^3.4.4",
    "mathjs": "^12.4.0",
    "plotly.js-dist-min": "^2.27.0",
    "unicodeit": "^0.7.5"
  }
}
